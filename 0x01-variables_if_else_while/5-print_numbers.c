#include <stdio.h>

/**
 * main - Entry point
 * print single digits of base 10 numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d\n", n);
	printf("\n");
	return (0);
}
