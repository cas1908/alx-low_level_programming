#include "main.h"

/**
 * main - Testing if a number is positive or negative
 *
 * Return: Always 0 (succes#include "main.h"
 */

void positive_or_negative(int i)
{
	int i;

	if (i > 0)
	{
		printf("%d is positive\n", i);
		return (0);
	}
	else (n < 0)
	{
		printf("%d is negative\n", i);
		return (0);
	}

}
